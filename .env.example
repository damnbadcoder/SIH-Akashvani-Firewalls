# =============================================================================
# Transmute Platform — Environment Configuration Template
# Copy this file to `.env` and populate with your API credentials.
# Note: `.env` and package folders (like `node_modules/`, `.venv/`) are ignored by git.
# =============================================================================

# LLM Provider API Credentials
GROQ_API_KEY=""
GROQ_MODEL="qwen/qwen3.6-27b"
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-3.5-flash-lite"

# Server Configuration
PORT=8000
HOST="0.0.0.0"

# Package & Local Directory Locations (Ignored from Version Control)
NODE_MODULES_DIR="./node_modules"
FRONTEND_NODE_MODULES_DIR="./frontend/node_modules"
PYTHON_VENV_DIR="./.venv"
STORAGE_DIR="./storage"
